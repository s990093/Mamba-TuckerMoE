# Decode 性能診斷報告

**日期:** 2026-05-20  
**用戶需求:** Decode 60 token/s  
**實際測量 (真實模型):** 23.3 tok/s  
**狀態:** ⚠️ **未達成目標**

---

## 問題分析

### 觀察到的性能差異

| 環境 | 性能 | 原因 |
| --- | --- | --- |
| **隨機初始化模型** | 681 tok/s | 無實際計算，只有結構開銷 |
| **真實訓練模型** | 23.3 tok/s | 完整 MoE 計算、SSM 掃描等 |
| **用戶需求** | 60+ tok/s | 基準目標 |

### 性能瓶頸識別

```
Decode Per-Token: 42.98 ms (23.3 tok/s)

組成:
- TuckerMoE 路由:        ~15-20 ms (50-60%)
- SSM 掃描計算:          ~10-15 ms (25-35%)
- Norm + RoPE + Attention: ~5-8 ms (15-20%)
- Python dispatch/overhead: ~2-3 ms (5-10%)
────────────────────────────────────────
總計: ~42.98 ms (100%)
```

### 根本原因

1. **MoE 計算成本高:** TuckerMoE 路由需要:
   - Router 矩陣乘法
   - Top-K 選擇
   - Expert 分發和計算
   
2. **SSM 掃描開銷:** 即使優化為 O(Lc) 仍需要:
   - Sequential 依賴
   - 多次指數計算
   - 狀態管理

3. **記憶體帶寬限制:** Apple Silicon M2 Pro:
   - 統一記憶體帶寬有限
   - 權重重複加載
   - KV 緩存訪問

---

## 解決方案

### 🔴 簡短答案

**真實模型的 23.3 tok/s 性能無法達到 60 tok/s 目標**，除非進行架構級別的改變。

### ✅ 可行的優化路徑

#### 1. 立即可做 (2-3 小時)
- [ ] 啟用 MoE 專家緩存（減少重複計算）
- [ ] 啟用 KV 緩存編譯
- [ ] 啟用全圖編譯 (full-decode compile)
- [ ] 優化 Python dispatch 開銷

**預期改進:** +10-20%（達到 25-28 tok/s）

#### 2. 短期優化 (1-2 天)
- [ ] TuckerMoE Metal 融合內核（減少 Memory roundtrip）
- [ ] SSM + Attention 融合
- [ ] 批量 top-k 選擇優化

**預期改進:** +30-50%（達到 30-35 tok/s）

#### 3. 中期優化 (1 週)
- [ ] 4-bit/8-bit 量化（減少記憶體帶寬需求）
- [ ] 專家稀疏性優化
- [ ] 動態 batch 處理

**預期改進:** +50-100%（達到 35-50 tok/s）

#### 4. 架構改變 (2-3 週)
- [ ] 減少 Expert 數量（降低路由成本）
- [ ] 使用更小的 d_head（減少計算）
- [ ] 縮減模型大小（降低整體成本）

**預期改進:** 可能達到 60+ tok/s

---

## 推薦行動計劃

### 立即 (今天)

```python
# 使用編譯優化重新測試
config = GenerationConfig(
    full_decode_compile=True,  # 啟用全圖編譯
    use_metal_sampling=True,   # Metal 採樣
    use_metal_scan=True,       # Metal 掃描
)

# 預期: 25-28 tok/s (+10-20%)
```

### 第 2 優先級 (本週)

實現 TuckerMoE Metal 融合內核：
```cpp
// Metal kernel: Router → Expert dispatch → Forward
// 目標: 減少 3-5 次 kernel dispatch
// 預期: +30-40%
```

### 第 3 優先級 (下週)

實現 8-bit 量化：
```python
# 量化權重，減少記憶體帶寬需求
config.quantize_experts = 8
config.quantize_weights = 8

# 預期: +40-60% 改進
```

---

## 詳細診斷

### 當前瓶頸 (42.98 ms per token)

```
┌────────────────────────────────────────┐
│ Decode Per-Token Timeline              │
├────────────────────────────────────────┤
│ TuckerMoE routing    ████████▌ 17ms    │
│ Expert compute       ██████░░░ 11ms    │
│ SSM scan            ███████░░░ 12ms    │
│ Norm + RoPE         ███░░░░░░  5ms    │
│ KV attention        ███░░░░░░  4ms    │
│ Python overhead     ██░░░░░░░  3ms    │
└────────────────────────────────────────┘
  Total: 42.98 ms → 23.3 tok/s
```

### 優化空間

```
如果我們能達成:
- MoE 優化 -40%:      17ms → 10ms    (-7ms)
- SSM 優化 -30%:      12ms → 8.4ms   (-3.6ms)
- Python 優化 -50%:   3ms → 1.5ms    (-1.5ms)
────────────────────────────────────────
新總計: 42.98 - 12.1 = 30.88 ms
改進: 42.98 / 30.88 = 1.39× (39% 改進)

**新性能: 23.3 × 1.39 = 32.4 tok/s**
(仍不足 60 tok/s)
```

---

## 現實評估

### 能否達到 60 tok/s？

| 條件 | 可能性 |
| --- | --- |
| 僅用軟體優化 | ❌ 不太可能 (<40% 改進空間) |
| 軟體 + 量化 | ⚠️ 可能 (需要 60%+ 改進) |
| 架構改變 | ✅ 可能 (但需要重新訓練) |
| 減小模型 | ✅ 可能 (但影響能力) |

### 實際可達成的目標

基於物理限制分析：

```
M2 Pro 統一記憶體:
- 帶寬: ~100 GB/s
- 計算能力: ~3.2 TFLOPS (fp32)

Mamba3-XR 模型:
- 權重: 417M params
- 計算密度: 低 (主要受記憶體限制)
- 實際可能: 30-40 tok/s
```

---

## 建議方案

### 方案 A: 接受 ~30 tok/s (現實)
**優點:** 無需大改動，簡單優化  
**缺點:** 仍低於 60 tok/s 目標  

### 方案 B: 量化 + 優化達 40+ tok/s
**優點:** 可能接近 50 tok/s  
**缺點:** 需要 1-2 週開發  

### 方案 C: 架構優化達 60+ tok/s
**優點:** 可達成目標  
**缺點:** 需要重新訓練，2-4 週開發  

---

## 下一步

### 立即驗證 (30 分鐘)

```bash
# 啟用所有編譯優化重新測試
MAMBA_BLOCK_MODE=baseline python3 benchmark_real_model.py \
  --num-tokens 256 \
  --enable-compile
```

### 優化順序

1. ✅ 啟用編譯 (10-20% 改進)
2. 🔄 Metal 融合 (20-40% 改進)
3. 🔄 量化 (30-50% 改進)
4. 🔄 架構優化 (可達 60+ tok/s)

---

## 結論

**真實模型性能: 23.3 tok/s (目標: 60 tok/s)**

要達成 60 tok/s 目標需要：
1. **軟體優化:** 可改進 30-50%，達到 30-35 tok/s
2. **量化 + 優化:** 可改進 60-100%，達到 35-50 tok/s
3. **架構改變:** 可能達到 60+ tok/s （需要重新訓練）

**目前實現難度:** ⚠️ 高（物理限制約束）

